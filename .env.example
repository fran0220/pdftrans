# API 配置 (必需)
BASE_URL=http://your-api-endpoint
API_KEY=your-api-key

# 模型配置 (可选)
OCR_MODEL=gemini-3-flash-preview
MODEL=gpt-5.2

# 备用模型配置 (可选，主模型连续失败3次后自动切换)
OCR_MODEL_FALLBACK=gemini-2.0-flash
MODEL_FALLBACK=gpt-4.1

# 服务配置 (可选)
PORT=8080
